<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nurlatif.submission">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Submission1">
        <activity android:name=".ui.home.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.searchMatch.SearchMatchActivity"/>
        <activity android:name=".ui.matchDetail.DetailMatchActivity" />
        <activity android:name=".ui.leaguehighlight.HighlightActivity" />
        <activity android:name=".ui.leagueDetail.LeagueDetailActivity" />
        <activity android:name=".ui.home.LeaguesActivity">

        </activity>
    </application>

</manifest>